# 🌐 Руководство по переводу описаний товаров

## 📝 Описание

Расширение поддерживает автоматический перевод описаний товаров на русский язык с помощью **Gemini API** от Google.

## 🔑 Получение API ключа

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в свой Google аккаунт
3. Нажмите "Create API Key"
4. Скопируйте полученный ключ

## ⚙️ Настройка

1. Откройте расширение в браузере
2. Перейдите во вкладку **"⚙️ Настройки"**
3. Вставьте ваш Gemini API ключ в поле "Gemini API Key"
4. Нажмите **"💾 Сохранить настройки"**
5. Дождитесь сообщения "✅ Настройки сохранены!"

## 🚀 Использование

1. Откройте страницу с товарами
2. В расширении включите чекбокс **"🌐 Переводить описания"**
3. Нажмите **"🔎 Глубокий парсинг товаров"**
4. Дождитесь завершения парсинга
5. Расширение автоматически переведет описания каждого товара

## 📊 Результат в SQL миграции

Переведенные описания сохраняются в отдельном поле `description_translated`:

```sql
INSERT INTO `products` (
  `product_id`, 
  `category_id`, 
  `status_id`, 
  `sort_id`, 
  `sku_code`, 
  `title`, 
  `screen_name`, 
  `photo`, 
  `price_whosale`, 
  `description`,              -- Оригинальное описание
  `description_translated`,   -- Переведенное описание
  `created`, 
  `updated`
) VALUES
(1, 0, 1, 0, 'ABC123', 'Product Name', 'product-name', '/storage/2025/1114/hash.jpg', 5000, 'Original English description...', 'Переведенное описание на русском...', 1731590400, 1731590400);
```

## 🎯 Особенности

- **Автоматическое определение языка**: Если текст уже на русском, он не будет переведен повторно
- **Обработка ошибок**: Если перевод не удался, сохраняется оригинальное описание
- **Безопасность**: API ключ хранится локально в `chrome.storage.local`
- **Производительность**: Перевод выполняется последовательно для каждого товара

## ⚠️ Важно

- API ключ **НЕ** хранится в коде расширения
- API ключ **НЕ** попадает в репозиторий (защищен `.gitignore`)
- Для перевода требуется активное подключение к интернету
- Gemini API имеет бесплатный лимит запросов (уточните на сайте Google)

## 🔒 Безопасность

Файл `.env` добавлен в `.gitignore` для защиты вашего API ключа:

```gitignore
# Environment variables
.env
.env.local
.env.*.local
```

**НИКОГДА** не публикуйте свой API ключ в открытом доступе!

## 💡 Советы

1. Перевод работает только при **"Глубоком парсинге товаров"**
2. Если чекбокс "🌐 Переводить описания" отключен, перевод не выполняется
3. Переведенное описание можно использовать для мультиязычного сайта
4. Оригинальное описание всегда сохраняется в поле `description`

## 🐛 Возможные ошибки

### "Ошибка Gemini API: 404" - model is not found
**Причина**: Использовалась неподдерживаемая или устаревшая модель
**Решение**: Исправлено в последней версии. Теперь используется стабильная модель `gemini-2.5-pro` через API v1beta. Перезагрузите расширение (chrome://extensions/ → 🔄).

### "⚠️ Для перевода необходимо установить API ключ в настройках"
**Решение**: Перейдите в настройки и сохраните API ключ

### "Ошибка Gemini API: 400"
**Причина**: Неправильный формат запроса или недействительный API ключ
**Решение**: 
1. Проверьте правильность API ключа (должен начинаться с `AIza...`)
2. Убедитесь, что ключ не содержит лишних пробелов
3. Получите новый ключ на [Google AI Studio](https://makersuite.google.com/app/apikey)

### "Ошибка Gemini API: 403"
**Причина**: API ключ недействителен или отключен
**Решение**: Создайте новый API ключ на [Google AI Studio](https://makersuite.google.com/app/apikey)

### "Ошибка Gemini API: 429"
**Причина**: Превышен лимит запросов
**Решение**: Подождите или обновите тарифный план на Google AI

### Перевод выполняется, но не попадает в SQL
**Решение**: 
1. Откройте консоль браузера (F12 → Console)
2. Найдите логи:
   - `🔑 API ключ из хранилища` - проверьте, что ключ загружен
   - `✅ Перевод сохранен для товара` - проверьте, что перевод выполнен
   - `✅ SQL: Товар имеет переведенное описание` - проверьте, что перевод добавлен в SQL
3. Если какого-то лога нет - проблема на этом этапе

### Перевод не выполняется
**Решение**: 
1. Убедитесь, что чекбокс "🌐 Переводить описания" включен
2. Проверьте наличие интернет-соединения
3. Откройте консоль браузера (F12 → Console) и проверьте логи
4. Убедитесь, что API ключ правильно сохранен в настройках

## 🔍 Отладка

### Логирование в консоли

Расширение выводит подробные логи в консоль браузера:

```
🔑 API ключ из хранилища: AIzaSyBcDe...
🌐 Перевод включен: true
📝 Переводим описание товара "Product Name": Original text...
✅ Gemini API ответ получен: {candidates: [...]}
✅ Перевод выполнен: Переведенный текст...
✅ Перевод сохранен для товара "Product Name"
✅ SQL: Товар "Product Name" имеет переведенное описание (150 символов)
```

### Проверка API ключа

Чтобы убедиться, что API ключ работает:

1. Откройте консоль браузера (F12)
2. Вставьте и выполните:

```javascript
chrome.storage.local.get(['geminiApiKey'], (result) => {
  console.log('Сохраненный ключ:', result.geminiApiKey);
});
```

### Тестовый запрос к API

Чтобы проверить, что API работает:

```javascript
const apiKey = 'ВАШ_API_КЛЮЧ';
const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-pro:generateContent?key=${apiKey}`;

fetch(apiUrl, {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    contents: [{parts: [{text: 'Translate to Russian: Hello world'}]}]
  })
})
.then(r => r.json())
.then(data => console.log('API Response:', data))
.catch(err => console.error('API Error:', err));
```

## ℹ️ Информация о моделях

Расширение использует модель **gemini-2.5-pro** (stable) через API v1beta, которая:
- ✅ Стабильная версия от Google (обновлена в июне 2025)
- ✅ Поддерживает `generateContent`
- ✅ Доступна бесплатно
- ✅ Лимиты: 1,048,576 входных токенов / 65,536 выходных токенов
- ✅ Работает для перевода текстов
- ✅ Не требует дополнительных настроек

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что API ключ действителен
3. Проверьте лимиты Gemini API на [Google AI Studio](https://makersuite.google.com/)

